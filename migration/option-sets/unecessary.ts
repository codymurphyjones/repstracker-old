export type AnalyticsAction = {
	display: string;
};

type Blog = {
	title: string;
};

type BlogPrivacy = 'LISTED' | 'UNLISTED' | 'PRIVATE';

type CouponCodeType = 'FREE' | 'DISCOUNT';

type FaqType = 'REPS/STR' | 'APP';

type OnboardingType = 'EXISTING' | 'NEW';
